import * as assert from 'assert';
import { PluralI18NEntry, SingleI18NEntry } from 'i18n-proto';
import { TranslationControllerTestable } from './utils';

describe('I18n end-user library', () => {
  it('all ok', () => {
    assert.ok(true);
  });
});

